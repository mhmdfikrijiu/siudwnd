services:
  narto:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - narto_cache:/data/narto_cache
    environment:
      - NARTO_CACHE=/data/narto_cache
      - NARTO_CACHE_MAX_MB=10240
      - PORT=5000
    restart: unless-stopped

volumes:
  narto_cache:
